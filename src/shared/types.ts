export interface Metadata {
    title: String,
    url: String,
    protocol: String,
    framework: String | null,
    cssFramework: String | null,
    charset: String,
    viewport: String,
    devicePixelRatio: Number,
    browser: String,
    platform: String,
    favicon: String,
    secure: boolean
}